{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {},
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        ".next/**",
        "!.next/cache/**",
        "dist/**"
      ]
    }
  }
}
